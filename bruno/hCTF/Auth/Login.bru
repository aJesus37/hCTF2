meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{host}}/api/collections/users/auth-with-password
  body: json
  auth: inherit
}

body:json {
  {
    "identity": "user@heaven-labs.com",
    "password": "userpassword123"
  }
}
