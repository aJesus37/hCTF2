- Add a way to people click in other people's profiles to have a similar view to that of viewing it's own profile
- Analyze all relevant aspects of the application and make it instrumented, add OTel metrics for important/relevant parts, aswell as logs. instead of how it is currently being handled.
- Create an OpenAPI Spec for this project. Every change that happens in any part of it should also be reflected in the OpenAPI spec. Save this memory to CLAUDE.md
- Add a Ctrl + Shift + F shortcut that will format the SQL code in the editor in both the /sql full playground and the partial template embedded in challenges. By formatting I mean new lines, indents, etc, make it beautiful
- Add the Ctrl + Enter to run the entire block
- Add the Ctrl + Shift + Enter to run only the current statement (separated by ;, ending SQL Clause)
- Make it clearer to the user that when a challenge has the embedded SQL thing, the data will reside in a table named `dataset`
- Add some kind of scrolling to big results in the SQL results, instead of showing everything anyway. Something like a selectable button that by default LIMITs at 1k rows, but can be disabled for more rows.
- Add a team page profile