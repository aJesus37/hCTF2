root = "."
tmp_dir = "tmp"

[build]
cmd = "CGO_ENABLED=0 go build -o app cmd/server/main.go"
bin = "./app serve --http='0.0.0.0:8090' --dev"
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["assets", "tmp", "vendor"]

[misc]
clean_on_exit = true
