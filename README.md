# hCTF

The Capture the Flag platform

## To Do

- [X] Use the PocketBase Record functions to interact with the database instead of direct querys for all paths
- [X] Use only pocketbase collections paths to deal with teams
- [ ] Make it possible to use alpine.js + htmx to authenticate and set a persistent Cookie/Authorization header
OR
- [ ] Make OIDC Work and add a default oidc provider like authentik but lighter to the thing. With a portal for creating accounts would be better.